export enum ProductSort {
    POPULAR = 1, // most popular first
    PRICE_UP, // from cheap to expensive
    PRICE_DOWN, // from expensive to cheap
    NEW, // most new first
}

export enum WishlistItemsSort {
    LASTEST = 1, // last edded
    PRICE_UP, // from cheap to expensive
    PRICE_DOWN, // from expensive to cheap
}
