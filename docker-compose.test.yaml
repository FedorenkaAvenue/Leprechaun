version: '3.8'

services:
  postgres:
    extends:
      file: docker-compose.override.yaml
      service: postgres
    restart: on-failure:5

  server:
    extends:
      file: docker-compose.override.yaml
      service: postgres
    restart: on-failure:5
    build:
      context: ./server
      dockerfile: Dockerfile.prod
    environment:
      - IS_DEV=false

  manticore:
    extends:
      file: docker-compose.override.yaml
      service: postgres
    restart: on-failure:5

  client:
    extends:
      file: docker-compose.override.yaml
      service: postgres
    restart: on-failure:5
    build:
      context: ./client
      dockerfile: Dockerfile.prod
    environment:
      - IS_DEV=false

  nginx:
    extends:
      file: docker-compose.override.yaml
      service: postgres
    restart: on-failure:5
    ports:
      - 84:80

  admin:
    extends:
      file: docker-compose.override.yaml
      service: postgres
    restart: on-failure:5
    build:
      context: ./admin
      dockerfile: Dockerfile.prod
