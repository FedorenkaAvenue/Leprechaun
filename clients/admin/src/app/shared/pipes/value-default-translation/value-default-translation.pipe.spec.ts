import { ValueDefaultTranslationPipe } from "./value-default-translation.pipe";

describe('ValueDefaultTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueDefaultTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
