import PropertyGroupListWidget from '@widgets/propertyGroup/ui/PropertyGroupTable';

const PropertyGroupTablePage = () => {
    return <PropertyGroupListWidget />;
};

export default PropertyGroupTablePage;
