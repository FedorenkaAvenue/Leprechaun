export enum ProductLabelModel {
    DISCOUNT = 'discount',
    NEW = 'new',
    POPULAR = 'popular',
}

export enum ProductSortModel {
    POPULAR = 1,
    PRICE_UP,
    PRICE_DOWN,
    NEW,
}

export enum ProductStatusModel {
    AVAILABLE = 1,
    OUT_OF_STOCK,
}
