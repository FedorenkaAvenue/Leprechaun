import { CurtainsDirective } from "./curtains.directive";


describe('CurtainsDirective', () => {
  it('should create an instance', () => {
    const directive = new CurtainsDirective();
    expect(directive).toBeTruthy();
  });
});
