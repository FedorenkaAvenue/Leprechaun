import { OrderStatusIndicatorDirective } from './order-status-indicator.directive';

describe('OrderStatusIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderStatusIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
