import { TranslocoBrowserLoader } from './transloco-browser-loader';

describe('TranslocoBrowserLoader', () => {
  it('should create an instance', () => {
    expect(new TranslocoBrowserLoader(null, null)).toBeTruthy();
  });
});
