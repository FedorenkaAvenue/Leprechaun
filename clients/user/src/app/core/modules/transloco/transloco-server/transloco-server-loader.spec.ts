import { TranslocoServerLoader } from './transloco-server-loader';

describe('TranslocoServerLoader', () => {
  it('should create an instance', () => {
    expect(new TranslocoServerLoader()).toBeTruthy();
  });
});
