syntax = "proto3";

package user;

service UserService {
  rpc FindOne (UserDTO) returns (User);
}

message UserDTO {
    string id = 1;
    string email = 2;
}

message User {
    string id = 1;
    int32 role = 2;
    string email = 3;
    string password = 4;
}
